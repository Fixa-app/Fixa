# Dev log — 16 juni 2026

## Sessie focus
V1 deadline: 3 juli. Prioriteiten: quote flow afmaken, dashboard echte data, request flow.

## Strategische beslissingen

### Foto's uit quote flow
Foto's verwijderd uit stap 2 van de quote flow. Foto's horen thuis op:
- **Request** — foto's van de situatie/het probleem (primair)
- **Job** — voor/na documentatie (secundair)

Quote heeft geen foto's in V1. Code opgeschoond.

### Pre-fill line items afgeschaft
Nieuwe offerte start met één leeg line item. Pre-fill via `products` tabel verwijderd uit `/api/quotes/create`. Beslissing na overleg: pro's offertes zijn te klus-specifiek voor zinvolle pre-fill. Terugbrengen in V1.1 na gebruikersgesprekken.

---

## Dashboard

### Workflow kaarten verwijderd
De vier workflow kaarten (Aanvragen/Offertes/Opdrachten/Facturen) waren dubbelop met de lijsten eronder. Verwijderd.

### Floating action button (mobile only)
Nieuwe `+` knop rechtsonder op mobile met vier opties:
- Aanvraag
- Offerte  
- Factuur
- Opdracht (disabled, "binnenkort")

Op desktop: `lg:hidden` — sidebar "Nieuw" dropdown is voldoende.

### Kaart styling
Dashboard secties (Offertes, Facturen) krijgen zelfde kaart-opmaak als Nieks workflow kaarten: `bg-card`, gekleurd icoon, zwarte tekst.

### Onboarding banner
Banner "Aan de slag 2/4" verwijderd van dashboard — staat al in sidebar. Sidebar is voldoende voor zowel desktop als mobile.

### Spacing
Meer ruimte tussen greeting en secties: `space-y-4` → `space-y-6`.

---

## Quote flow stap 3 — Preview + Versturen

### Setup checklist
Als logo of offertenummer ontbreekt bij aanvangen stap 3:
- Twee witte kaarten bovenaan (geen border)
- Logo kaart: upload knop met context, success state na upload
- Offertenummer kaart: context "Het offertenummer op de offerte die je deelde was: 2025010029", leeg input veld
- Preview gedempt (`opacity-40 pointer-events-none`) totdat beide ingevuld zijn
- Verstuurknop disabled totdat setup compleet

### Preview document
- Bedrijfsinfo + logo
- Klantgegevens + offertenummer + datum + vervaldatum (datum + 30 dagen)
- Aanhef met tokens resolved
- Line items tabel
- Totalen (subtotaal, BTW per tarief, totaal incl. BTW)
- Disclaimer met tokens resolved

### Tokens resolved in preview
- `[klantnaam]` → klantnaam uit stap 1
- `[adres]` → adres uit stap 1
- `[offertedatum + 30 dagen]` → datum + 30 dagen

### Versturen
Bij klikken "Offerte versturen":
- Offertenummer toegewezen aan quote
- Status → `awaiting_response`
- `sent_at` opgeslagen
- `next_quote_number` verhoogd met 1
- Email: nog niet gebouwd (wacht op Resend API key + DNS hifixa.com)

### Opslaan als concept
CTA "Opslaan als concept" navigeert naar `/dashboard/quotes`.

---

## Offertenummering

### Aanpak
Pro vult volledig offertenummer in (bijv. `2025010030`). Opgeslagen als integer in `next_quote_number`. Format `{YEAR}-{NUMBER}` niet meer toegepast in weergave — pro's eigen nummer wordt exact getoond.

### DB
```sql
ALTER TABLE quotes ADD COLUMN quote_number TEXT;
```

---

## Onboarding completion tracking

### Fixes
- Layout en onboarding page gebruiken nu service role client voor logo + offertenummer check (RLS bypass)
- Cookie fallback: als geen `active_company_id` cookie, gebruik eerste company van user
- Import conflict opgelost: `createClient` aliassen `createServerClient` en `createServiceClient`

### Storage fixes
```sql
GRANT ALL ON quotes TO authenticated; -- storage trigger had geen rechten op quotes tabel
```

---

## Settings

### Offertenummer weergave
`formatQuoteNumber()` verwijderd uit weergave — toont nu raw `next_quote_number` waarde.

---

## Openstaand

- Dashboard: mockup data vervangen door echte Supabase queries
- Request flow bouwen
- Email versturen via Resend (wacht op DNS + API key van Niek)
- Quotes list: echte data
- Clients pagina
- Invoices list
- Google Places Autocomplete adresveld
- Magic link werkt niet op mobile emulator
- Onboarding mobile layout (Nieks fix nodig)

## V1.1 product visie (gelogd vorige sessie)
- AI foto interpretatie per line item via Claude Vision + skills-docs per vakgebied
- Supplier flow: pro filmt werk onderaannemer → AI line item → onderaannemer geeft prijs
- Pre-fill line items terugbrengen na gebruikersgesprekken
