# Dev log — 6 juni 2026

## Stap 2 new quote flow — vereenvoudigd

**Verwijderd:**
- Leverancier checkbox (was disabled, V2)
- Marge velden (V2 — complexiteit vereist tonen van oorspronkelijk bedrag vs marge)

**Verbeterd:**
- Meer ruimte tussen line items op mobile
- Label toegevoegd aan job title veld
- Auto-resize textarea voor aanhef en disclaimer (groeit mee met inhoud)

---

## Token-systeem voor aanhef en disclaimer

**Beslissing:** Aanhef en disclaimer werken met dynamische tokens die bij versturen vervangen worden door werkelijke waarden.

**Tokens:**
- `[klantnaam]` → naam van de klant uit stap 1
- `[adres]` → adres van de klant uit stap 1
- `[offertedatum + 30 dagen]` → datum van versturen + 30 dagen

**Onboarding save route:**
- `quote_intro` altijd opgeslagen als standaard template met tokens
- `quote_disclaimer` parsed tekst bewaard, maar vervaldatum vervangen door `[offertedatum + 30 dagen]`

**Bestaande accounts geüpdatet:**
- Alderbouw: `quote_intro` bijgewerkt naar template met tokens
- Fokker (niels+2@lucen.tech): zowel intro als disclaimer bijgewerkt

---

## Stap 1 new quote flow — fixes

**Kapitalisatie bij opslaan:**
- `capitalizeNL()` wordt nu ook toegepast bij het daadwerkelijk opslaan van de klantnaam, niet alleen in de UI
- Nederlandse tussenvoegsels (`van`, `de`, `den`, etc.) krijgen geen hoofdletter

**Duplicate client check:**
- Nieuwe `exact` query parameter op `/api/quotes/clients` voor exacte case-insensitive match
- "Nieuwe klant aanmaken" knop verborgen als er een exacte match in de zoekresultaten staat
- Als pro toch op aanmaken klikt: wordt bestaande klant geselecteerd in plaats van duplicate aangemaakt

---

## Onboarding — line item classificatie getest

**Test met GP Fokker offerte (loodgieters/tegelwerk):**
- Parser herkent 3 items: Tegelwerk groot formaat, Stucwerk trappenhuis, Schilderwerk trappenhuis
- Alle drie correct als `labor` geclassificeerd
- Parkeerkosten staan in begeleidende tekst, niet als line item — correct gedrag
- "Terugkerende posten" sectie verschijnt correct in onboarding stap 3
- Geen "Overige posten" sectie — correct want geen `other` items op deze offerte

**Bevinding:** Pro's offerte bevat klus-specifieke bedragen, niet standaard tarieven. Pro past bedragen aan in stap 2. Dit is het verwachte gedrag.

---

## DB wijzigingen

```sql
-- item_type kolom toegevoegd aan products tabel
ALTER TABLE products ADD COLUMN item_type text DEFAULT 'other';

-- Bestaande Alderbouw producten geclassificeerd
UPDATE products SET item_type = 'labor' WHERE title IN ('Voorbereiding en werkzaamheden', 'Stukwerk wand');
UPDATE products SET item_type = 'material' WHERE title = 'Materiaal';
UPDATE products SET item_type = 'transport' WHERE title = 'Parkeerkosten';
UPDATE products SET item_type = 'other' WHERE title = 'Aanbetaling 50%';

-- Grants voor service role
GRANT ALL ON clients, quotes, line_items, invoices TO service_role;
```

`database.types.ts` geregenereerd.

---

## Openstaand

- Stap 3: preview + versturen (tokens resolven naar werkelijke waarden)
- Dashboard: mockup data vervangen door echte Supabase queries
- Clients pagina (`/dashboard/customers`)
- Invoices list view
- Google Places Autocomplete voor adresveld
- Magic link werkt niet op mobile emulator en soms op echte iPhone — onderzoeken
- Onboarding mobile layout kapot (Nieks wijziging) — aan Niek te melden
